enum FeatureFlags {
    /// When true: shows the original chat UI with message bubbles and mode toggle.
    /// When false: shows the realistic walkie-talkie radio UI (no text, just commands).
    static let classicUI: Bool = false
}
